@php /** @var \LaravelSyndication\Feeds\Structure\FeedCloud $cloud */ @endphp
<cloud domain="{{ $cloud->domain }}"
       port="{{ $cloud->port }}"
       path="{{ $cloud->path }}"
       registerProcedure="{{ $cloud->procedure }}"
       protocol="{{ $cloud->protocol }}" />
